version: "3.8"

services:
  text-appeal:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    volumes:
      - app-data:/app/server/data
    restart: unless-stopped

volumes:
  app-data:
